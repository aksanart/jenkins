pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "build progress."'
                sh 'echo "build completed."'
            }
        }
    }
}